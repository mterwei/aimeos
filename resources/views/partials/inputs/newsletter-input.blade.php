<section class="newsletter-subscription">
    <form action="#">
        <div class="subscribe">
            <input type="text" name="newsletter" placeholder="{{__('home.newsletter_placeholder')}}"/>
            <button>
                <span class="button small secondary">{{__('home.subscribe')}}</span>
            </button>
        </div>
    </form>
</section>
